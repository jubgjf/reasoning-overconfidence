```shell
$ uv pip install -r requirements.txt
$ uv pip install -r requirements-server.txt --find-links https://flashinfer.ai/whl/cu124/torch2.5/flashinfer-python
```


```shell
$ export PYTHONPATH=$(pwd)
$ python scripts/download-dataset.py
```
